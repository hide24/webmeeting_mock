<%= form_with(model: users_external_service) do |form| %>
  <% if users_external_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_external_service.errors.count, "error") %> prohibited this users_external_service from being saved:</h2>

      <ul>
        <% users_external_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :user_id, class: "form-label" %>
    <%= form.text_field :user_id, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :external_service_id, class: "form-label" %>
    <%= form.text_field :external_service_id, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :external_mail, class: "form-label" %>
    <%= form.text_field :external_mail, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
    <%= link_to 'Back', users_external_services_path, class: "btn btn-outline-secondary" %>
  </div>
<% end %>
